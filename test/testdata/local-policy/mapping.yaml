# map repos to policies
version: v1
kind: policy-mapping
policies:
  - origin:
      domain: docker.io
      prefix: library/
    name: test-images
    description: Local test images
    files:
      - path: doi/data.yaml
      - path: doi/policy.rego

mirrors:
  - name: test-images
    mirror:
      domains: [localhost:5001]
      prefix: ""
